export type SpotraneAlbumCardView = {
    id: string;
    name: string;
    releaseDate: string;
    imageUri: string
    albumUri: string
    label: string
    artistId: string
    artistName: string
    artistGenres: string[]
    isSaved: boolean
}